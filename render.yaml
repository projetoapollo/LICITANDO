render.yamlservices:
  - type: web
    name: LICITANDO
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run script_principal_turbo.py --server.port=$PORT --server.address=0.0.0.0"
    pythonVersion: 3.10.8
